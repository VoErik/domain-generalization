# Model and dataset
model: "ResNet18"
dataset: "PACS"
dataset_dir: "datasets"

# Training hyperparameters
lr: 1e-3
batch_size: 32
momentum: 0.9 # not used
weight_decay: 0
epochs: 50
eps: 1e-8
betas: [0.9, 0.999]

# Optimization and loss
criterion: "cross_entropy"
optimizer: "sgd"
nesterov: None # not used

# Early stopping and scheduling
use_early_stopping: true
patience: 8
use_scheduling: true

# Device and pretraining
device: "cuda"
pretrained: true

# Experiment details
log_dir: "experiments"
experiment: "resnet34-pt-randaugment-pacs"
num_runs: 10

# Augmentations
mixstyle_layers: None
mixstyle_alpha: 0.3
mixstyle_p: 0.5
mix_type: "random"
visualize_latent: false
use_mixup: false
use_cutmix: false
use_augmix: false
use_randaugment: true
mixup_alpha: 0.3
cutmix_alpha: 0.2
augmix_severity: 3
num_ops: 1